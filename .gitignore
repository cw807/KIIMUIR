# 비밀값 — 절대 커밋 금지
secrets.json
.env

# 수집 데이터 — 영업 정보이므로 공개 저장소에 올리지 않는다.
# 로컬에는 그대로 쌓이고, 깃에만 올라가지 않는다.
data/

# 파이썬
__pycache__/
*.pyc
.venv/

# Playwright 브라우저 (용량 큼)
browsers/

# 임시 조회 파일
r*.json
wf.json
repo.json
runs.json
